build/
dist/

# The compiled binary: a build artifact, and it BAKES the Google OAuth
# client id/secret in from client_credentials.mk — committing it
# publishes them (GitHub push protection caught exactly that).
tasks
# `make app` builds into dist/ (ignored above); this only guards a stray
# bundle left at the repo root.
Tasks.app/

# The LIVE config: per-machine values, and it holds the OAuth refresh
# token.  Only tasks.ini.defaults belongs in git (see the "Config"
# convention in CLAUDE.md).  Kept as an anchored pattern so it cannot
# match tasks.ini.defaults.
/tasks.ini

# The rename migrations write a verbatim copy of the old ini before
# rewriting it — same secrets as the live file, refresh token and all,
# so they must be just as uncommittable.  The bare *.bak rule is the
# backstop: no editor or script backup belongs in this repo.
/tasks.ini.pre-4.0.bak
/tasks.ini.pre-3.0.bak
*.bak

# Pre-rename names (Hacienda → Lists → Tasks), still ignored so an old
# build, config or database left in a working copy cannot be committed.
# /lists.ini in particular SURVIVES the 4.0 rename: the migration copies
# it to tasks.ini and deliberately leaves the original in place.
lists
Lists.app/
/lists.ini
/lists.ini.pre-3.0.bak
hacienda
hacienda.ini

client_credentials.mk
.DS_Store

# Agent worktrees live INSIDE the repo, so `git add` turns one into a
# phantom submodule (a mode-160000 gitlink, no .gitmodules) that GitHub
# renders as a broken folder.  Ignore the parent so it cannot happen
# again — this does not disturb a live worktree checked out there.
.claude/worktrees/

# clangd compilation database — machine-specific include paths
# (regenerated by `make`).
compile_commands.json
.cache/

# The Google OAuth client file (downloaded from the Cloud console) —
# credentials must never be committed.
client_secret*.json
